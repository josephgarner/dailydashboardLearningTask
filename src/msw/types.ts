export type Weather = {
  forcast: string;
  max: number;
  min: number;
  description: string;
};

export type News = {
  title: string;
  image: string;
  description: string;
  datePosted: Date;
};
